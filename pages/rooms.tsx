import { RoomBanner, SectionThree, SectionTwo } from '@/components/UI';

const rooms = () => {
  return (
    <div>
      <RoomBanner />
      <SectionTwo />
      <SectionThree />
    </div>
  );
};

export default rooms;
