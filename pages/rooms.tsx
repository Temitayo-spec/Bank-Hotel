import { Header, RoomBanner, SectionThree, SectionTwo } from '@/components/UI';

const rooms = () => {
  return (
    <div>
      <Header />
      <RoomBanner />
      <SectionTwo />
      <SectionThree />
    </div>
  );
};

export default rooms;
